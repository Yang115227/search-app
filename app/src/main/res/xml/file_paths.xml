<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- 应用缓存目录（debug 截图、临时文件） -->
    <cache-path
        name="cache"
        path="." />

    <!-- 应用内部文件目录 -->
    <files-path
        name="files"
        path="." />

    <!-- 应用外部文件目录（日志导出） -->
    <external-files-path
        name="external_downloads"
        path="Download/" />
</paths>